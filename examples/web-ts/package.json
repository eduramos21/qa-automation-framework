{
  "name": "qa-framework-example-web-ts",
  "private": true,
  "description": "Runnable ts-playwright demo. Points at saucedemo.com so the README examples actually execute.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
